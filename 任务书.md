# 2025-2026-2学期《C++现代程序设计》课程-项目任务书
本文为《C++现代程序设计》期末大作业需求，学生需完成项目分析、设计、编码、测试并撰写项目报告。

---

## 一、项目开发组织形式
1. 以**小组形式**完成，每组**2~4人**，自由组合并推选组长。
2. 小组成员分工明确，确定各自任务量。
3. 项目成果由组长统一提交。

---

## 二、项目成果提交
1. 按报告模板撰写项目报告，命名规则：
  `2025-2026-2 学期《现代C++程序设计》课程-项目报告-小组名或姓名.docx`
2. 所有源代码（`.cpp`、`.h/.hpp`、数据文件、`Makefile`）放入同一目录，**不包含可执行文件**。
3. 运行环境配置写入`config.md`，存放于源代码目录，需包含操作系统、编译环境等信息。

---

## 三、评价依据
按项目完成度、代码质量、报告质量综合评价，出现以下情况**得0分**：
1. 未按要求完成。
2. 逾期提交。
3. 项目代码或报告缺失任一。
4. 项目代码存在明显抄袭。
5. 报告格式不合格。

---

## 附件1：项目要求
### 一、内容和要求
运用C++相关知识，对标MySQL，开发**微型关系型数据库管理系统**。
- 团队开发，建议使用Vibe Coding技术完成全部代码。
- 自行编写单元测试代码并通过测试。

### 二、系统功能要求
#### （一）具体功能要求
##### 1. 存储功能
基于宿主操作系统文件系统实现，数据表、索引以文件存储，目录结构与文件格式自定。
**示例目录结构**：
```
data // 根目录
├── person // 数据库目录
│   ├── person.dat // 表文件
│   └── person.idx // 索引文件
└── other // 其他数据库
```

##### 2. DDL（数据定义语言）
至少实现以下语句：
1. **create database**
   - 功能：创建数据库
   - 语法：`create database <dbname>`
   - 示例：`create database person`

2. **drop database**
   - 功能：删除数据库
   - 语法：`drop database <dbname>`

3. **use**
   - 功能：切换数据库
   - 语法：`use <dbname>`

4. **create table**
   - 功能：创建表
   - 语法：
     ```sql
     create table <table-name> ( 
       <column> <type> [primary], 
       …
     )
     ```
   - 约束：表名、列名为全英文小写，无下划线与特殊字符。
   - 支持类型：`int`（C++默认长度）、`string`（UTF-8编码，最长256字符定长串）。
   - 主键约束：指定`primary`需用**B+树**建立索引。

5. **drop table**
   - 功能：删除表及对应索引（如有）
   - 语法：`drop table <table-name>`

> 注：自行设计错误处理逻辑。

##### 3. DML（数据操作语言）
至少实现以下语句：
1. **select**
   - 功能：条件查询表数据并显示结果
   - 语法：`select <column> from <table> [ where <cond> ]`
   - 说明：
     - `<column>`：单个列名或`*`（全部列）。
     - `<cond>`：`<column> <op> <const-value>`。
     - `<op>`：支持`=`、`<`、`>`。
   - 优化：有索引则使用索引。

2. **delete**
   - 功能：按条件删除表记录
   - 语法：`delete <table> [ where <cond> ]`

3. **insert**
   - 功能：插入数据
   - 语法：`insert <table> values (<const-value>[, <const-value>…])`
   - 约束：字符串用**双引号**包裹。

4. **update**
   - 功能：按条件更新记录，无条件则更新全表
   - 语法：`update <table> set <column> = <const-value> [ where <cond> ]`
   - 约束：字符串用**双引号**包裹。

> 注：可在最低要求基础上扩展语句功能。

#### （二）界面要求
模仿MySQL交互式CLI界面，示例操作：
```
> create table person (id int primary, name string) 
反馈信息
> use person 
> insert person values(1001, "peter") 
反馈信息
> select name from person where id = 1001 
反馈信息
> exit
```
反馈信息需清晰明确。

---

### 三、编码要求
#### （一）程序结构
采用**C/S架构**，服务端与客户端分离：
1. 通信协议：建议使用TCP/IP。
2. 客户端：用户登录，CLI输入SQL语句，发送至服务端，接收结果并格式化显示。
3. 服务端：解析执行SQL，返回结果集，支持自定义数据格式（如JSON，需实现序列化/反序列化）。

**建议核心类**：
| 序号 | 类(模板)名 | 说明 |
| ---- | ---------- | ---- |
| 1    | column     | 表的列 |
| 2    | row        | 表的行，column容器 |
| 3    | table      | 表，row容器；结果集可用table子集存储 |
| 4    | index      | 表索引，B+树实现 |
| 5    | database   | 数据库 |

需自行设计：SQL解析器、执行引擎、存储引擎、网络引擎、单元测试等模块。

#### （二）代码编写与构建
1. 建议使用Vibe Coding技术。
2. **禁止使用C++ STL标准容器**（array/vector/list/map/set等），需自行实现。
3. 可使用基础组件、多线程库、第三方网络库。
4. C++标准：**至少C++20，推荐C++23**。
5. 开发环境：**Linux系统**。
6. 构建工具：**CMake**。
7. 代码托管：建议使用Git（推荐Gitee）。

#### （三）报告撰写
可使用AI辅助生成，需严格遵循报告模板。

---

## 附件2：项目报告模板
另附文件。

要不要我帮你把这份**项目任务书**再整理成一份**可直接打印的精简版**？
# MiniDB 版本记录

## v1.3.1 (2026-05-25)

### 修复：测试代码编译错误与级联删除测试逻辑
- **[修复]** `tests/test_main.cpp:436-437`：`rs.columns[0] == "id"` 编译错误——`Column` 对象与 `const char*` 字符串字面量无匹配的 `operator==`，修正为 `rs.columns[0].name == "id"`
- **[修复]** `tests/test_main.cpp` CASCADE 测试：在执行级联删除前增加清理非 CASCADE 外键引用行的步骤，修复因 `employees` 表未解除引用阻塞导致 CASCADE 测试失败的逻辑错误
- 测试用例总数从 60 增至 61，全部通过
- **[更新]** `README.md`：测试结果更新至 61/61
- 本次为纯测试代码修复，核心功能未变更

## v1.3.0 (2026-05-25)

### 新增：外键约束
- **CREATE TABLE 支持外键**：`foreign key (col) references parent_table(col) [on delete cascade]`
- **INSERT 外键校验**：插入子表时自动检查外键值在父表中是否存在，不存在则拒绝
- **UPDATE 外键校验**：更新外键列时检查新值在父表中是否存在
- **DELETE 引用检查**：删除父表行时，若有子表通过非 CASCADE 外键引用则拒绝删除
- **CASCADE 级联删除**：`ON DELETE CASCADE` 在删除父表行时自动删除子表关联行
- **DROP TABLE 保护**：有子表通过外键引用时禁止删除父表，必须先删除子表
- 外键定义持久化到 `schema.json`，以 `FK:` 标记存储在表定义行末尾
- 新增 18 个外键相关单元测试（2 个解析测试 + 16 个执行测试）
- 总测试用例从 42 增至 60，全部通过

### 变更详情
- [新增] `src/common/Column.h`：ForeignKeyDef 结构体（column, refTable, refColumn, onDeleteCascade）
- [修改] `src/parser/ASTNode.h`：CreateTableNode 添加 `ArrayList<ForeignKeyDef> foreignKeys` 字段
- [修改] `src/parser/SQLParser.h`：parseCreateTable() 解析 FOREIGN KEY 语法，支持 ON DELETE CASCADE
- [修改] `src/storage/StorageEngine.h`：saveTableSchema() 支持外键持久化；新增 loadTableForeignKeys()、getReferencingTables() 方法
- [修改] `src/storage/Table.h`：新增 existsByKey()、findMatchingRows() 方法用于 FK 校验
- [修改] `src/executor/Executor.h`：executeCreateTable() 校验外键引用；executeInsert() 插入前 FK 检查；executeUpdate() 更新外键列时检查；executeDelete() 删除时检查引用完整性并支持级联删除
- [新增] `tests/test_main.cpp`：testForeignKey() 18 个外键测试用例
- [更新] `README.md`：添加外键语法说明、测试结果更新至 60/60
- [更新] `项目报告.md`：添加外键约束相关内容
- [更新] 版本从 v1.2.0 升级至 v1.3.0

## v1.2.0 (2026-05-25)

### 新增：CLI 增强功能
- **多行输入**：以 `;` 为语句结束标志，支持跨行输入（提示符自动切换到 `->` 续入模式）
- **多条语句批处理**：用 `;` 分隔多条 SQL 语句，客户端逐条发送并逐条显示结果
- **命令历史**：基于 GNU Readline 库，↑/↓ 箭头键浏览历史命令，支持行内编辑（←/→/Ctrl+A/E/U）
- 构建依赖：新增 `libreadline-dev`，`CMakeLists.txt` 链接 `-lreadline`

### 变更详情
- [重构] `src/network/Client.h`：重写 CLI 交互逻辑，集成 GNU Readline 实现历史命令，支持 `;` 结尾多行输入和多语句批处理
- [更新] `CMakeLists.txt`：客户端链接 `-lreadline`
- [更新] `README.md`：添加使用技巧章节（多行输入/多语句批处理/命令历史）、更新环境要求
- [更新] `项目报告.md`：添加 CLI 增强功能介绍

## v1.1.0 (2026-05-25)

### 新增：视图模块
- **视图创建**：`create view <name> as select <cols> from <table> [where <cond>]`
- **视图删除**：`drop view <name>`
- **视图查询**：`select *|cols from <viewname> [where <cond>]`
- 视图定义持久化到 `schema.json`，以 `VIEW:` 前缀标识
- 查询视图时自动展开为底层表查询，支持视图 WHERE 条件与用户 WHERE 条件组合
- 新增 9 个视图相关单元测试（3 个解析测试 + 6 个执行测试）
- 总测试用例从 33 增至 42，全部通过

### 变更详情
- [新增] `src/common/Common.h`：StatementType 枚举增加 CREATE_VIEW、DROP_VIEW
- [新增] `src/parser/ASTNode.h`：CreateViewNode、DropViewNode 结构体
- [新增] `src/parser/SQLParser.h`：parseCreateView() 方法，CREATE/DROP VIEW 解析分支
- [新增] `src/storage/StorageEngine.h`：ViewDefinition 结构体，saveViewDefinition()、loadViewDefinition()、viewExists()、removeViewDefinition() 方法
- [新增] `src/executor/Executor.h`：executeCreateView()、executeDropView()、executeSelectFromView() 方法
- [新增] `tests/test_main.cpp`：9 个视图测试用例
- [更新] `README.md`：添加视图操作说明、视图模块详解、测试结果更新
- [更新] `项目报告.md`：添加视图模块相关内容
- [更新] 版本从 v1.0.0 升级至 v1.1.0

## v1.0.0 (2026-05-25)

### 初始化版本
- 实现 C/S 两层架构的微型关系型数据库
- TCP 网络通信（端口 23333），JSON 协议序列化
- SQL 解析器支持 DDL（CREATE/DROP/USE DATABASE/TABLE）和 DML（INSERT/SELECT/UPDATE/DELETE）
- 自定义动态数组容器 ArrayList
- B+树索引优化主键等值查询
- Value 序列化/反序列化（定长存储：INT 4字节，STRING 257字节）
- 存储引擎管理数据库/表文件（schema.json 元数据）
- 33 个单元测试覆盖全部核心功能

### 变更详情
- [修复] ResultSet::format()：保留 `"X rows inserted"` 等消息字符串
- [修复] ResultSet::fromJson()：正确解析 JSON 中的 columns 和 rows 数组
- [修复] ResultSet::fromJson()：rows 数组括号深度追踪 bug，外层 `[]` 剥离导致第一行丢失
- [修复] ResultSet::format()："1 rows in set" → "1 row in set" 单复数
- [修复] Table.h insert/update/delete：返回 "1 row inserted"/"2 rows updated" 等正确的单复数消息
- [修复] Table.h insert()：使用 C 风格 `fopen/fwrite/fclose` 替代 `fstream app` 模式写入数据
- [修复] test_readme.sh：清理路径从 `build/data/school` 修正为 `data/school`
- [更新] README.md 输出示例匹配实际运行效果
- [新增] test_readme.sh 自动化集成测试脚本

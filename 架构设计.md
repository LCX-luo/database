# 微型关系型数据库管理系统 - 架构设计

## 一、总体架构（C/S架构）

```
┌──────────────────┐         TCP/IP          ┌──────────────────────┐
│   Client (CLI)   │ ◄─────────────────────► │      Server          │
│                  │      JSON over TCP      │                      │
│ 用户输入SQL语句   │                          │  接收SQL → 解析 → 执行 │
│ 显示查询结果      │                          │  返回结果集给客户端    │
└──────────────────┘                          └──────────────────────┘
```

## 二、模块划分

### 2.1 公共服务（common/）
| 类/模块 | 职责 |
|---------|------|
| `ArrayList<T>` | 自定义动态数组，替代 std::vector |
| `Value` | 数据值类型，支持 int 和 string |
| `Column` | 表的列定义：名称、类型、是否主键 |
| `Row` | 表中的一行数据 |
| `ResultSet` | 查询结果集，多行数据 + 列信息 |

### 2.2 存储引擎（storage/）
| 类/模块 | 职责 |
|---------|------|
| `StorageEngine` | 文件级I/O管理，读写表数据和索引文件 |
| `Database` | 数据库管理（create/drop/use database） |
| `Table` | 表管理（create/drop table，insert/select/update/delete 数据） |

### 2.3 索引模块（index/）
| 类/模块 | 职责 |
|---------|------|
| `BPlusTree` | B+树索引实现，支持插入、查找（点查/范围查）、删除 |

### 2.4 SQL解析器（parser/）
| 类/模块 | 职责 |
|---------|------|
| `ASTNode` | 抽象语法树节点基类及派生类 |
| `SQLParser` | 将SQL文本解析为AST，支持词法分析+语法分析 |

### 2.5 执行引擎（executor/）
| 类/模块 | 职责 |
|---------|------|
| `Executor` | 接收AST，调用存储引擎执行操作，返回结果集 |

### 2.6 网络模块（network/）
| 类/模块 | 职责 |
|---------|------|
| `Socket` | TCP Socket封装，跨平台相关 |
| `Server` | 服务端：监听连接，接收SQL，发送结果 |
| `Client` | 客户端：连接服务端，发送SQL，接收并显示结果 |

## 三、数据流

```
用户输入SQL
    │
    ▼
Client (CLI界面) ────TCP/JSON────► Server
                                      │
                                      ▼
                                  SQLParser (词法分析→语法分析→AST)
                                      │
                                      ▼
                                  Executor (语义分析→执行计划)
                                      │
                            ┌─────────┼─────────┐
                            ▼         ▼         ▼
                        Storage    BPlusTree   ResultSet
                        Engine      (索引)      (缓存)
                            │
                            ▼
                    文件系统 (data/目录)
```

## 四、存储结构

```
data/                           # 数据根目录
├── <dbname>/                  # 数据库目录
│   ├── <tablename>.dat        # 表数据文件（二进制定长记录）
│   └── <tablename>.idx        # B+树索引文件
│   └── schema.json            # 数据库/表元数据
└── ...
```

## 五、自定义容器

由于任务要求**禁止使用STL容器**，本项目自行实现：

- **`ArrayList<T>`**：动态数组容器，支持 push_back、pop_back、at、size、clear、迭代器 等操作
- 允许使用 `std::string`、`iostream`、`fstream`、`thread`、`cstring` 等基础库

## 六、B+树设计

- 阶数（order）：固定为 4（每个节点最多4个键，5个指针）
- 内部节点：存储键和子节点指针
- 叶子节点：存储键和记录位置（文件偏移量）
- 支持操作：insert、search（精确/范围）、remove

## 七、SQL语法支持

### DDL
- `create database <dbname>`
- `drop database <dbname>`
- `use <dbname>`
- `create table <name> (<col> <type> [primary], ...)`
- `drop table <name>`

### DML
- `insert <table> values(<val>, ...)`
- `select <col>|<columns>|* from <table> [where <col> <op> <val>]`
- `update <table> set <col>=<val> [where <cond>]`
- `delete <table> [where <cond>]`

## 八、网络协议

- 基于TCP，端口号 23333
- 通信格式：JSON字符串
  - 请求：`{"sql": "..."}`
  - 响应：`{"code": 0, "message": "...", "columns": [...], "rows": [[...], ...]}`
- 使用 `\n` 作为消息结束标志

## 九、构建系统

- CMake + Make
- C++20 标准
- 编译选项：-Wall -Wextra -std=c++20

# 2025-2026-2学期《C++现代程序设计》课程-项目报告
电子科技大学信息与软件工程学院

---

| 文件状态<br/>[  ]草稿 [  ]正式发布 [✔]正在修改 | 文件标识 | MiniDB-REPORT-001 |
| :--- | :--- | :--- |
|  | 当前版本 | 1.2 |
|  | 小组成员 | （学号） | （姓名） |
|  |  | （学号） | （姓名） |
|  |  | （学号） | （姓名） |
|  |  | （学号） | （姓名） |
|  | 完成日期 | 2026-05-25 |

---

## 版本历史
| 版本/状态 | 作者 | 参与者 | 起止日期 | 备注 |
| :--- | :--- | :--- | :--- | :--- |
| 1.3/正式发布 | 项目组 | 全体成员 | 2026-05-25 | 外键约束（参照完整性、级联删除），60个测试全部通过 |
| 1.2/正式发布 | 项目组 | 全体成员 | 2026-05-25 | CLI增强（多行输入、多语句、历史命令），视图模块 |
| 1.1/正式发布 | 项目组 | 全体成员 | 2026-05-25 | 新增视图模块，42个测试全部通过 |
| 1.0/正式发布 | 项目组 | 全体成员 | 2026-05-10 ~ 2026-05-25 | 初始版本完成 |

---

# 目录
1. 第一章 引言
    1.1 项目背景
    1.2 术语与缩写解释
2. 第二章 系统需求
3. 第三章 系统设计
4. 第四章 系统实现
5. 第五章 系统测试
6. 第六章 结语
7. 参考文献

---

# 第一章 引言

## 1.1 项目背景

随着大数据时代的到来，关系型数据库管理系统（RDBMS）在数据存储和管理中扮演着至关重要的角色。MySQL作为业界最受欢迎的开源关系型数据库之一，其核心设计思想——SQL解析、查询优化、存储引擎、索引机制等——构成了现代数据库系统的基石。

本课程项目旨在通过从零开发一个微型关系型数据库管理系统（MiniDB），深入理解数据库系统的核心原理与实现技术。项目对标MySQL的功能子集，实现了从SQL解析、查询执行到数据持久化的完整链路。

MiniDB采用C/S（客户端/服务器）架构，基于TCP/IP协议进行通信，客户端提供交互式CLI界面，服务端负责SQL解析和执行。项目使用C++20标准开发，在Ubuntu Linux环境下构建，使用CMake作为构建工具。存储层基于宿主文件系统实现，支持B+树索引、二进制定长记录存储，功能上覆盖了DDL（数据定义语言）和DML（数据操作语言）的核心语句。

本项目的开发过程遵循软件工程方法论，依次进行了需求分析、架构设计、模块实现、单元测试等阶段，确保系统的正确性和可靠性。全部代码均使用Vibe Coding技术（AI辅助编码）完成，显著提升了开发效率。

## 1.2 术语与缩写解释
| 术语/缩写 | 解释 |
| :--- | :--- |
| RDBMS | 关系型数据库管理系统（Relational Database Management System） |
| DDL | 数据定义语言（Data Definition Language），用于定义数据库结构 |
| DML | 数据操作语言（Data Manipulation Language），用于操作数据 |
| DQL | 数据查询语言（Data Query Language），用于查询数据 |
| SQL | 结构化查询语言（Structured Query Language） |
| AST | 抽象语法树（Abstract Syntax Tree），SQL解析的中间表示 |
| C/S架构 | 客户端/服务器架构（Client/Server Architecture） |
| TCP/IP | 传输控制协议/网际协议（Transmission Control Protocol/Internet Protocol） |
| B+树 | 一种平衡多路查找树，常用于数据库索引实现 |
| CLI | 命令行界面（Command Line Interface） |
| JSON | JavaScript对象表示法（JavaScript Object Notation），一种轻量级数据交换格式 |
| OOD | 面向对象设计（Object-Oriented Design） |
| OOA | 面向对象分析（Object-Oriented Analysis） |

---

# 第二章 系统需求

## 2.1 系统总体目标

MiniDB的目标是开发一个微型关系型数据库管理系统，对标MySQL的核心功能，支持基本的数据库管理、表管理和数据操作功能。系统采用C/S架构，通过TCP/IP协议实现客户端与服务端的分离通信，提供友好的交互式CLI操作界面。

## 2.2 功能需求

### 2.2.1 系统功能结构

系统的功能结构分为客户端和服务端两大部分，如图2-1所示。

```
┌─────────────────────────────────────────────────────────┐
│                     MiniDB系统                           │
├──────────────────────┬──────────────────────────────────┤
│    客户端模块         │          服务端模块               │
├──────────────────────┼──────────────────────────────────┤
│  CLI交互界面          │  SQL解析器                       │
│  SQL语句发送          │  执行引擎                        │
│  结果格式化显示       │  存储引擎                        │
│  TCP网络通信         │  B+树索引                        │
│                      │  TCP网络通信                      │
└──────────────────────┴──────────────────────────────────┘
```

### 2.2.2 DDL功能

系统需实现以下DDL语句：

1. **create database**：创建数据库，在文件系统中创建对应的目录
2. **drop database**：删除数据库，删除对应的目录及其所有文件
3. **use**：切换当前使用的数据库
4. **create table**：创建表，支持int和string两种数据类型，支持指定主键
5. **drop table**：删除表及对应的索引文件
6. **create view**：创建视图，基于 SELECT 查询定义的虚拟表
7. **drop view**：删除视图定义

### 2.2.3 DML功能

系统需实现以下DML语句：

1. **insert**：向表中插入数据，支持int和string类型
2. **select**：条件查询，支持列选择（*表示全部列）和where条件过滤
3. **update**：按条件更新数据
4. **delete**：按条件删除数据

查询条件支持=、<、>三种操作符，对于主键等值查询使用B+树索引优化。

### 2.2.4 界面需求

模仿MySQL交互式CLI界面，提供"minidb> "输入提示，接收用户输入的SQL语句，执行后显示格式化反馈信息（如"Query OK, X rows affected"）或表格形式的结果集。客户端需支持以下增强交互功能：

1. **多行输入**：以 `;` 作为语句结束标志，输入未结束时提示符切换为 `->` 续入模式
2. **多条语句批处理**：支持在一行或多行中以 `;` 分隔多条语句，系统逐条执行并分别显示结果
3. **命令历史**：支持 ↑/↓ 箭头键浏览历史输入过的 SQL 语句，支持行内编辑（基于 GNU Readline 库）

## 2.3 非功能需求

1. **性能**：支持B+树索引加速主键查询
2. **可靠性**：数据持久化到文件系统，支持异常恢复
3. **可扩展性**：模块化设计，便于功能扩展
4. **兼容性**：C++20标准，Linux环境运行

---

# 第三章 系统设计

## 3.1 系统架构设计

MiniDB采用经典的C/S（客户端/服务器）两层架构，如图3-1所示。

```
┌──────────────────────┐         TCP/IP          ┌──────────────────────────┐
│   Client (CLI)       │ ◄─────────────────────► │       Server             │
│                      │      JSON over TCP      │                          │
│  用户输入SQL语句     │                          │  接收SQL → 解析 → 执行   │
│  显示查询结果         │                          │  返回结果集JSON          │
└──────────────────────┘                          └──────────────────────────┘
```

**通信协议**：基于TCP协议，使用JSON格式进行数据交换。请求格式为`{"sql":"..."}`，响应格式为`{"code":0,"message":"...","columns":[...],"rows":[[...],...]}`。使用换行符`\n`作为消息结束标志。

## 3.2 模块设计

系统划分为以下核心模块：

### 3.2.1 公共服务模块（common/）

| 类名 | 职责 |
|:---|:---|
| `ArrayList<T>` | 自定义动态数组容器，替代 std::vector |
| `Value` | 数据值类型，支持 int 和 string 两种类型 |
| `Column` | 表的列定义：名称、数据类型、是否主键 |
| `Row` | 表中的一行数据，是 Value 的容器 |
| `ResultSet` | 查询结果集，支持格式化输出和JSON序列化 |

### 3.2.2 存储引擎模块（storage/）

| 类名 | 职责 |
|:---|:---|
| `StorageEngine` | 文件级I/O管理，数据库目录创建/删除，表元数据读写 |
| `Table` | 表级操作，数据文件的读写，支持索引查询优化 |

### 3.2.3 索引模块（index/）

| 类名 | 职责 |
|:---|:---|
| `BPlusTree` | B+树索引实现，支持插入、查找（精确/范围）、删除、遍历 |

### 3.2.4 SQL解析器模块（parser/）

| 类名 | 职责 |
|:---|:---|
| `ASTNode` | 抽象语法树节点基类及派生类（每种SQL语句对应一个节点类） |
| `SQLParser` | SQL词法分析和语法分析，将SQL文本解析为AST |

### 3.2.5 视图模块

| 概念 | 说明 |
|:---|:---|
| **视图** | 一种虚拟表，其内容由存储的 SELECT 查询定义，不占用物理存储空间 |
| **视图定义存储** | 在 schema.json 中以 `VIEW:` 前缀标识，格式：`VIEW:viewname,col1,col2,...,sourcetable[,condcol,condop,condval]` |
| **视图展开** | 查询视图时，Executor 检测表名是否为视图，自动加载视图定义并展开为底层表查询 |
| **条件组合** | 支持视图定义中的 WHERE 条件与用户查询的 WHERE 条件组合使用 |

### 3.2.6 执行引擎模块（executor/）

| 类名 | 职责 |
|:---|:---|
| `Executor` | 接收AST，调用存储引擎执行操作，返回结果集，负责视图展开查询 |

### 3.2.7 网络通信模块（network/）

| 类名 | 职责 |
|:---|:---|
| `Socket` | TCP Socket封装，支持服务端和客户端 |
| `Server` | 服务端程序，监听连接、接收SQL、返回结果 |
| `Client` | 客户端程序，CLI交互、发送SQL、显示结果 |

## 3.3 类图设计

MiniDB的核心类关系如图3-2所示（采用OOD方法设计）。

```
┌──────────────────┐     ┌──────────────────┐
│    SQLParser     │     │    Executor      │
├──────────────────┤     ├──────────────────┤
│ +parse(sql):AST  │────>│ +execute(AST):RS  │
└──────────────────┘     └────────┬─────────┘
                                  │
                  ┌───────────────┼───────────────┐
                  ▼               ▼               ▼
          ┌────────────┐  ┌────────────┐  ┌────────────┐
          │ StorageEngine│ │   Table   │  │ BPlusTree │
          ├────────────┤  ├────────────┤  ├────────────┤
          │ +createDB()│  │ +insert() │  │ +insert() │
          │ +dropDB()  │  │ +select() │  │ +search() │
          │ +useDB()   │  │ +update() │  │ +remove() │
          │ +saveSchema│  │ +remove() │  │ +getAll() │
          └────────────┘  └────────────┘  └────────────┘
                                  │
                                  ▼
                          ┌──────────────┐
                          │  文件系统     │
                          │  (data/目录)  │
                          └──────────────┘
```

## 3.4 存储结构设计

数据在文件系统中的组织结构如下：

```
data/                           # 数据根目录
├── <dbname>/                   # 数据库目录
│   ├── <tablename>.dat        # 表数据文件（二进制定长记录）
│   ├── <tablename>.idx        # B+树索引文件
│   └── schema.json            # 元数据文件
```

- 表数据文件采用二进制定长记录格式，每行数据包含类型标记和值
- 索引文件存储B+树节点的序列化数据
- schema文件以CSV格式存储所有表的元数据

## 3.5 B+树索引设计

B+树阶数(order)固定为4，即每个节点最多存储4个键值对。主要设计要点：

- **内部节点**：存储键和子节点指针（文件偏移）
- **叶子节点**：存储键和记录位置（数据文件偏移），叶子节点之间通过nextLeaf指针链接形成链表
- **节点持久化**：B+树节点直接序列化到索引文件中，支持持久化和恢复
- **支持的操**：insert（自动分裂）、search（精确查找）、searchRange（范围查找）、remove、getAll（中序遍历获取全部）

## 3.6 Vibe Coding说明

本项目全程使用AI辅助编码（Vibe Coding）技术完成。具体采用：

- **AI模型**：DeepSeek V4 代码生成模型
- **开发环境**：VS Code + Remote SSH
- **工作流程**：需求分析 → AI辅助生成代码 → 人工审核和调优 → 编译测试 → 迭代修复

---

# 第四章 系统实现

## 4.1 自定义ArrayList容器

由于任务要求禁止使用STL标准容器，本项目自行实现了`ArrayList<T>`动态数组容器。核心实现如下：

```cpp
template<typename T>
class ArrayList {
private:
    T* data_;
    size_t capacity_;
    size_t size_;

    void reserve(size_t new_cap) {
        if (new_cap <= capacity_) return;
        T* new_data = new T[new_cap];
        for (size_t i = 0; i < size_; ++i) {
            new_data[i] = data_[i];
        }
        delete[] data_;
        data_ = new_data;
        capacity_ = new_cap;
    }

public:
    void push_back(const T& value) {
        if (size_ >= capacity_) {
            reserve(capacity_ == 0 ? 4 : capacity_ * 2);
        }
        data_[size_++] = value;
    }

    void erase_at(size_t index) {
        for (size_t i = index; i < size_ - 1; ++i) {
            data_[i] = data_[i + 1];
        }
        --size_;
    }
};
```

实现要点：使用原始指针和动态内存分配，初始容量为4，以2倍速率扩容。提供了push_back、pop_back、at、operator[]、find、erase_at、insert_at等核心操作，并实现了前向迭代器。

## 4.2 B+树索引实现

B+树是本项目的核心数据结构，负责主键索引的建立和查询优化。核心逻辑如下：

```cpp
void BPlusTree::insert(int key, int64_t dataOffset) {
    if (rootOffset_ < 0) {
        // 根节点为空，创建根节点
        BPlusNode root;
        root.isLeaf = true;
        root.keys[0] = key;
        root.dataOffsets[0] = dataOffset;
        root.keyCount = 1;
        rootOffset_ = allocateNode(file);
        root.fileOffset = rootOffset_;
        writeNode(file, root);
    } else {
        BPlusNode root;
        readNode(file, rootOffset_, root);
        if (root.keyCount == BPLUS_ORDER) {
            // 根节点已满，分裂
            BPlusNode newRoot;
            newRoot.isLeaf = false;
            newRoot.children[0] = rootOffset_;
            splitChild(file, newRoot, 0);
            rootOffset_ = allocateNode(file);
            newRoot.fileOffset = rootOffset_;
            writeNode(file, newRoot);
            // 重新确定插入位置
            int i = 0;
            while (i < newRoot.keyCount && newRoot.keys[i] < key) ++i;
            BPlusNode child;
            readNode(file, newRoot.children[i], child);
            insertNonFull(file, child, key, dataOffset);
        } else {
            insertNonFull(file, root, key, dataOffset);
        }
    }
}
```

B+树节点分裂流程：当节点键数达到阶数上限时，将节点分裂为两个，中位数键上提到父节点。对于叶子节点，分裂后通过nextLeaf指针保持链表连接。

## 4.3 SQL解析器实现

SQL解析器采用手写的递归下降解析方式，通过词法分析和语法分析两个步骤完成。解析`create table`语句的核心逻辑：

```cpp
std::unique_ptr<ASTNode> SQLParser::parseCreate(
    const std::string& original, 
    const std::string& lower, 
    std::stringstream& ss) {
    
    std::string keyword;
    ss >> keyword;  // "database" 或 "table"
    
    if (keyword == "database") {
        // 解析 create database <dbname>
        auto node = std::make_unique<CreateDatabaseNode>();
        ss >> node->dbName;
        return node;
    }
    
    if (keyword == "table") {
        // 解析 create table <name> (<col> <type> [primary], ...)
        auto node = std::make_unique<CreateTableNode>();
        ss >> node->tableName;
        
        // 提取括号中的列定义
        size_t parenStart = original.find('(');
        size_t parenEnd = original.find(')');
        std::string colsStr = original.substr(parenStart + 1, 
                                               parenEnd - parenStart - 1);
        // 解析每个列定义...
        return node;
    }
    return nullptr;
}
```

解析器支持的语句类型：create/drop database、use、create/drop table、create/drop view、insert、select、update、delete、exit。

## 4.4 执行引擎实现

执行引擎（Executor）接收AST节点，将其转换为实际的数据库操作。以select语句为例：

```cpp
ResultSet Executor::executeSelect(const SelectNode& node) {
    // 获取当前数据库和表信息
    ArrayList<Column> columns = storage_.loadTableSchema(dbName, node.tableName);
    Table table(dbName, node.tableName, columns, dataPath, idxPath);
    
    // 解析where条件
    Operator op = Operator::NONE;
    Value whereVal;
    if (node.condition.hasCondition) {
        op = getOperator(node.condition.op);
        // 根据列类型解析值
        whereVal = parseValue(node.condition.value, columns[colIdx].type);
    }
    
    // 执行查询
    return table.select(node.columns, node.condition.column, op, whereVal);
}
```

对于主键等值查询，Table::select方法会利用B+树索引直接定位记录位置，避免全表扫描。

## 4.5 视图模块实现

视图模块是本项目在 v1.1.0 版本中新增的功能，实现了数据库视图的创建、查询和删除。

### 4.6.1 视图解析

在 SQL 解析器中新增了对 `CREATE VIEW` 和 `DROP VIEW` 语句的支持。`parseCreateView()` 方法解析 `CREATE VIEW <name> AS SELECT <cols> FROM <table> [WHERE <cond>]` 语法，构建 `CreateViewNode` AST 节点：

```cpp
std::unique_ptr<ASTNode> SQLParser::parseCreateView(
    const std::string& original,
    const std::string& lower,
    std::stringstream& ss) {
    std::string viewName;
    ss >> viewName;
    // 读取 "as"
    std::string asKw;
    ss >> asKw;
    // 读取 "select"
    std::string selectKw;
    ss >> selectKw;
    
    auto node = std::make_unique<CreateViewNode>();
    node->viewName = viewName;
    // 解析 select 列（直到 "from"）
    // 解析 where 条件
    return node;
}
```

### 4.6.2 视图存储

视图定义持久化在数据库的 schema.json 文件中，以 `VIEW:` 前缀标识。存储引擎新增了以下方法：

- **saveViewDefinition()**：保存视图定义到 schema.json
- **loadViewDefinition()**：从 schema.json 加载视图定义
- **viewExists()**：检查视图是否存在
- **removeViewDefinition()**：删除视图定义

视图定义格式示例：
```
VIEW:good_student,id,name,age,student,age,=,20
```

### 4.6.3 视图查询展开

执行引擎在 `executeSelect()` 方法中增加了视图检测逻辑：当查询的表名是一个视图时，调用 `executeSelectFromView()` 方法，将视图展开为底层表查询：

```cpp
ResultSet Executor::executeSelectFromView(
    const std::string& dbName,
    const SelectNode& node) {
    // 加载视图定义
    ViewDefinition vd = storage_.loadViewDefinition(dbName, node.tableName);
    
    // 确定要查询的列
    // 如果用户查询 *，使用视图定义的列
    // 否则使用用户指定的列
    
    // 组合 WHERE 条件：先使用视图定义条件，再用用户查询条件覆盖
    // ...
    
    // 对底层表执行查询
    return table.select(queryColumns, whereColumn, op, whereVal);
}
```

视图模块的核心设计思想是将视图作为"存储的查询"处理，在执行时展开为对底层表的直接查询，这保证了视图数据始终与源表数据保持一致（实时视图）。

## 4.6 网络通信模块

基于POSIX Socket API实现TCP通信，服务端核心逻辑：

```cpp
class Server {
    bool start() {
        serverSocket_.createServer(port_);
        while (running_) {
            Socket clientSocket = serverSocket_.accept();
            // 处理客户端连接
            std::string sql = receiveSQL(clientSocket);
            ResultSet result = executeSQL(sql);
            clientSocket.send(result.toJson());
        }
    }
};
```

客户端采用交互式CLI模式：

```
minidb> create database person
Query OK
minidb> use person
Database changed
minidb> create table person (id int primary, name string)
Query OK
minidb> insert person values(1001, "peter")
Query OK, 1 rows affected
minidb> select * from person
+------+-------+
| id   | name  |
+------+-------+
| 1001 | peter |
+------+-------+
1 rows in set
minidb> exit
Bye
```

---

# 第五章 系统测试

## 5.1 测试环境

- 操作系统：Ubuntu 22.04 (Linux 6.8)
- 编译器：GCC 13.4.0 (C++20)
- 构建工具：CMake 3.22.1

## 5.2 单元测试

本项目编写了42个单元测试用例，覆盖所有核心模块，包括新增的视图模块。测试框架采用自定义的简单断言框架，无需第三方依赖。

### 5.2.1 测试用例及结果

| 测试模块 | 测试用例 | 测试结果 |
|:---|:---|:---:|
| ArrayList | 基本操作（push/pop/size/empty） | PASS |
| ArrayList | 查找和删除（find/erase_at/remove） | PASS |
| ArrayList | 拷贝和赋值构造 | PASS |
| ArrayList | 迭代器遍历 | PASS |
| Value | int类型创建和转换 | PASS |
| Value | string类型创建和转换 | PASS |
| Value | 比较操作（==, <, >） | PASS |
| Value | 序列化/反序列化 | PASS |
| SQL解析器 | CREATE DATABASE | PASS |
| SQL解析器 | DROP DATABASE | PASS |
| SQL解析器 | USE | PASS |
| SQL解析器 | CREATE TABLE（含列解析） | PASS |
| SQL解析器 | DROP TABLE | PASS |
| SQL解析器 | INSERT | PASS |
| SQL解析器 | SELECT（含*号） | PASS |
| SQL解析器 | SELECT with WHERE | PASS |
| SQL解析器 | UPDATE | PASS |
| SQL解析器 | DELETE | PASS |
| SQL解析器 | EXIT | PASS |
| B+树 | 插入和精确查找 | PASS |
| B+树 | getAll中序遍历 | PASS |
| 集成测试 | 创建数据库 | PASS |
| 集成测试 | 切换数据库 | PASS |
| 集成测试 | 创建表 | PASS |
| 集成测试 | 插入数据 | PASS |
| 集成测试 | SELECT * | PASS |
| 集成测试 | SELECT with WHERE条件 | PASS |
| 集成测试 | UPDATE数据 | PASS |
| 集成测试 | UPDATE后SELECT验证 | PASS |
| 集成测试 | DELETE数据 | PASS |
| 集成测试 | DELETE后SELECT验证 | PASS |
| 集成测试 | DROP TABLE | PASS |
| 集成测试 | DROP DATABASE | PASS |
| 视图测试 | CREATE VIEW 解析 | PASS |
| 视图测试 | CREATE VIEW with WHERE 解析 | PASS |
| 视图测试 | DROP VIEW 解析 | PASS |
| 视图测试 | CREATE VIEW（创建视图定义） | PASS |
| 视图测试 | SELECT FROM view（视图查询） | PASS |
| 视图测试 | SELECT FROM view with WHERE（视图带条件查询） | PASS |
| 视图测试 | CREATE VIEW with WHERE clause（创建带条件视图） | PASS |
| 视图测试 | SELECT from view with view WHERE（视图条件过滤） | PASS |
| 视图测试 | DROP VIEW（删除视图） | PASS |

**测试结果：42/42 全部通过。**

## 5.3 集成测试

通过模拟完整的用户操作流程，验证系统各模块间的协同工作：

1. 启动服务端，监听TCP端口23333
2. 客户端连接，执行：create database testdb
3. use testdb → create table → insert数据 → select查询 → update更新 → delete删除 → drop table → drop database
4. 服务端返回正确的JSON格式结果
5. 所有操作验证通过

---

# 第六章 结语

## 6.1 项目总结

本项目成功实现了一个微型关系型数据库管理系统（MiniDB），对标MySQL的核心功能子集。项目完整涵盖了从SQL解析、查询执行到数据持久化的全链路，实现了DDL（7种语句）和DML（4种语句）共11种SQL操作（含 CREATE/DROP VIEW），以及外键约束（参照完整性校验、级联删除）、B+树索引、二进制定长记录存储、视图模块、TCP网络通信、CLI增强交互（多行输入、多语句批处理、命令历史）等关键技术。

在开发过程中，团队严格遵循软件工程规范，依次完成了需求分析、架构设计、模块编码、单元测试等环节。项目采用OOD方法进行面向对象设计，模块划分清晰，各模块职责明确，具有良好的可扩展性。

## 6.2 项目不足与改进方向

1. **功能完备性**：当前仅支持int和string两种数据类型，可扩展支持float、double、date等更多类型
2. **查询优化**：仅对主键等值查询使用索引优化，可引入查询代价估算和更完善的优化策略
3. **视图功能**：当前视图为简单视图（不支持对视图 INSERT/UPDATE/DELETE），可扩展支持可更新视图
4. **并发控制**：当前服务端为单线程串行处理，可引入多线程和事务机制支持并发访问
5. **错误处理**：可进一步完善错误处理逻辑，提供更友好的错误提示
6. **SQL语法**：可扩展支持JOIN、GROUP BY、ORDER BY、子查询、CHECK约束等高级SQL特性
7. **外键功能**：当前外键约束未实现ON UPDATE CASCADE/SET NULL，未实现多列复合外键，可扩展支持

## 6.3 心得体会

通过本项目，团队成员深入理解了关系型数据库系统的核心原理，包括SQL解析的语法分析机制、B+树索引的数据结构设计与实现、二进制定长记录的存储格式、TCP网络通信协议等关键技术。同时，项目实践也锻炼了团队协作能力、系统设计能力和代码调试能力。

---

# 参考文献

[1] 杨粒,孙秀桂,余国庆,等.基于神经网络的植物图像识别[J].广州城市职业学院学报,2025,19(02):96-100.

[2] Silberschatz A, Korth H F, Sudarshan S. Database System Concepts[M]. 7th ed. McGraw-Hill Education, 2019.

[3] Cormen T H, Leiserson C E, Rivest R L, et al. Introduction to Algorithms[M]. 4th ed. MIT Press, 2022.

[4] MySQL 8.0 Reference Manual[EB/OL]. Oracle Corporation. https://dev.mysql.com/doc/refman/8.0/en/.

[5] Graefe G. Query Evaluation Techniques for Large Databases[J]. ACM Computing Surveys, 1993, 25(2): 73-169.

[6] Bayer R, McCreight E. Organization and Maintenance of Large Ordered Indices[J]. Acta Informatica, 1972, 1(3): 173-189.

---

# 附1：评分表
## C++项目评分表
### 1. 学生自评
请小组组长填写此表。

| 成员 | 学号 | 承担任务量 | 学生自评等级 |
| :--- | :--- | :--- | :--- |
| （姓名） | （学号） | 25% | A |
| （姓名） | （学号） | 25% | A- |
| （姓名） | （学号） | 25% | B+ |
| （姓名） | （学号） | 25% | B+ |

**注**
1) 承担任务量用百分比。标红内容为示例
2) 等级为A+/A/A-/B+/B/B-/C+/C/C-/D+/D/F，其中A+最好，F最差；只有F为不合格等级。
3) 自评等级将作为教师评价的参考。

### 2. 教师评价
下表由教师填写

| 项目完成度 | 报告质量 | 代码质量 |
| :--- | :--- | :--- |
|  |  |  |

**注**：最终成绩中，项目完成度占70%，报告质量占15%，代码质量占15%。

教师签字：白忠建

---

# 附2：报告撰写格式要求
## 1、标题
1. 字体：中文宋体，西文Times New Roman
2. 字号：1级标题三号黑体，2级和3级标题四号，粗体
3. 行间距：固定值，20磅
4. 缩进：按tab键

## 2、正文
1. 字体：中文宋体，西文Times New Roman
2. 字号：小四号
3. 行间距：1.5倍行距
4. 缩进：按tab键
5. 对齐：两端对齐

**Tips**：选中所有文字，先选择字体为宋体，再选择Times New Roman。

## 3、图表
1. 字体：中文宋体，西文Times New Roman
2. 字号：五号
3. 行间距：单倍行距，上下6磅
4. 对齐：居中
5. 位置：表头在表格上方；图题在图下方。另，表格根据窗口宽度自动调整。
6. 编号：格式为：章号-章内序号。例如：表1-2 xxx，图2-3 yyy。
7. 引用：表头和图题必须在正文中有引用，例如：如图2-3所示。

## 4、代码
1. 不用截图，粘贴文本
2. 字体：Consolas
3. 字号：小五号
4. 行间距：固定值，12磅
5. 加框。选中要加框的代码，选择"开始菜单"，在工具图标栏中部可以看到框线图标，点击它，在下拉列表中选择外侧框线。

**例**
```cpp
void SocketError( int nErrorCode )
{
    CString errmsg;

    switch ( nErrorCode )
    {
        case WSANOTINITIALISED:
            errmsg = "WSANOTINIT";
            break;
    }
}
```

**Tip**：粘贴代码前，在粘贴位置按Del，直到光标移动到文档最左侧，然后再粘贴，这样可以保持代码原来的缩进格式。此后再调整字体等。

注意：不按格式写的报告评分至少要降一个等级，甚至评为不合格！
